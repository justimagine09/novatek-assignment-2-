import { DropdownIdToTextPipe } from './dropdown-id-to-text.pipe';

describe('DropdownIdToTextPipe', () => {
  it('create an instance', () => {
    const pipe = new DropdownIdToTextPipe();
    expect(pipe).toBeTruthy();
  });
});
